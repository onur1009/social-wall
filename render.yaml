services:
  - type: web
    name: social-wall
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    region: frankfurt
    healthCheckPath: /api/health
    envVars:
      - key: XPOZ_API_KEY
        sync: false   # Dashboard'dan girilecek — güvenli
